{
  "name": "atlas-vpn",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm --prefix web ci && npm --prefix web run build",
    "start": "npm --prefix web start"
  },
  "engines": {
    "node": ">=20"
  }
}
